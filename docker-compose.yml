networks:
  shared_net:
    external: true

services:
  multiport_calc:
    build: .
    container_name: multiport_calc
    env_file:
      - .env
    networks:
      - shared_net
    expose:
      - "8001"
    restart: unless-stopped
    command: gunicorn -w 4 -b 0.0.0.0:8001 src:app